def findMedianSortedArrays(nums1, nums2 ):
    # import pdb; pdb.set_trace()
    if not len(nums1) :
        num2_median = 0
    if not len(nums2) :
        nums2_median = 0

    median = sum(sorted_array) / len(sorted_array)
    return median

nums1 = [1]
nums2 = []
print(findMedianSortedArrays(nums1, nums2))